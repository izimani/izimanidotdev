---
import Markdown from "../../util/markdown";

type Props = {
  class?: string;
  content: string;
};

const { class: className, content } = Astro.props;
---

<div class={"prose " + (className ?? "")} set:html={Markdown.render(content)} />
